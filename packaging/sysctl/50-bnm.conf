# bnm: allow unprivileged ICMP echo (ping) sockets so bnmd can measure latency without root.
# NixOS, Arch and Fedora already ship this default; Debian and Ubuntu do not.
net.ipv4.ping_group_range = 0 2147483647
